{% extends '::container.html.twig' %}


{% block content %}
<div id="inside_content">
    <div class="list_primary_buttons">
        <a href="{{ path('tax_new') }}" class="btn btn-primary"><i class="icon-plus"></i> {{ 'New tax'|trans }}</a>
    </div>

    <div class="page-header">
        <h1>{{ 'Tax list'|trans }}</h1>
    </div>

    <table class="table table-bordered table-hover table-striped" style="margin: 25px; width: 97%;">
        <thead>
            <tr>
                <th>{{ 'Name'|trans }}</th>
                <th>{{ 'Value'|trans }}</th>
                <th>{{ 'Is active ?'|trans }}</th>
                <th>{{ 'Is default ?'|trans }}</th>
                <th>{{ 'Actions'|trans }}</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('tax_edit', { 'id': entity.id }) }}">{{ entity.name }}</a></td>
                <td>{{ entity.value }}</td>
                <td>{{ entity.active }}</td>
                <td>{{ entity.isdefault }}</td>
                <td>
                    {% if entity.active %}
                        <a href="{{ path('tax_disable', { 'id': entity.id }) }}" class="btn btn-danger"><i class="icon-remove-circle"></i> {{ 'Disable'|trans }}</a>
                    {% else %}
                        <a href="{{ path('tax_enable', { 'id': entity.id }) }}" class="btn btn-success"><i class="icon-play-circle"></i> {{ 'Enable'|trans }}</a>
                    {% endif %}
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
</div>
{% endblock %}
