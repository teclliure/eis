<table  class="table table-bordered table-hover" style="margin: 25px; width: 97%;">
    <thead>
    <tr>
        <th {% if quotes.isSorted('q.number') %} class="sorted"{% endif %}>{{ knp_pagination_sortable(quotes, 'Quote number'|trans, 'q.number', {}, {}, 'TeclliureInvoiceBundle:Common:sortable_link.html.twig') }}</th>
        <th {% if quotes.isSorted('c.customer_name') %} class="sorted"{% endif %}>{{ knp_pagination_sortable(quotes, 'Customer name'|trans, 'c.customer_name', {}, {}, 'TeclliureInvoiceBundle:Common:sortable_link.html.twig') }}</th>
        <th {% if quotes.isSorted('q.created') %} class="sorted"{% endif %}>{{ knp_pagination_sortable(quotes, 'Created date'|trans, 'q.created', {}, {}, 'TeclliureInvoiceBundle:Common:sortable_link.html.twig') }}</th>
        <th {% if quotes.isSorted('q.status') %} class="sorted"{% endif %}>{{ knp_pagination_sortable(quotes, 'Status'|trans, 'q.status', {}, {}, 'TeclliureInvoiceBundle:Common:sortable_link.html.twig') }}</th>
        <th>{{ 'Total'|trans }}</th>
        <th class="clear"></th>
    </tr>
    </thead>
    <tbody>
    {% for quote in quotes %}
        <tr>
            <td>
                {{ quote.number }}
            </td>
            <td>{{ quote.common.customerName }}</td>
            <td>{{ quote.created|date('d/m/Y') }}</td>
            <td>{{ quote.statusName|trans }}</td>
            <td>{{ quote.common.grossAmount|number_format(2, '.', ',') }} €</td>
            <td>
                {% if quote.status == 0  %}
                <a href="{{ path('quote_edit', {'id': quote.common.id }) }}" alt="{{ 'Edit'|trans }}" class="btn btn-small"><i class="icon-edit"></i> {{ 'Edit'|trans }}</a>
                <a href="{{ path('quote_close', {'id': quote.common.id }) }}" alt="{{ 'Close'|trans }}" class="btn btn-small"><i class="icon-folder-close"></i> {{ 'Close'|trans }}</a>
                {% endif %}
                <div style="margin: 5px 0">
                {% if quote.status == 1 %}
                    {% if not quote.status < 3 %}
                        <a href="{{ path('quote_order', {'id': quote.common.id }) }}" alt="{{ 'Make order'|trans }}" class="btn btn-mini btn-success"><i class="icon-file"></i> {{ 'Make order'|trans }}</a>
                    {% endif %}
                    {% if not quote < 4 %}
                        <a href="{{ path('quote_invoice', {'id': quote.common.id }) }}" alt="{{ 'Make invoice'|trans }}" class="btn btn-mini btn-success"><i class="icon-file"></i> {{ 'Make invoice'|trans }}</a>
                    {% endif %}
                    <br />
                    {% if not quote.common.deliveryNote and not quote.common.invoice %}
                        <a href="{{ path('quote_deny', {'id': quote.common.id }) }}" alt="{{ 'Denied'|trans }}" class="btn btn-mini btn-danger"><i class="icon-lock"></i> {{ 'Denied'|trans }}</a>
                    {% endif %}
                {% endif %}
                </div>
                {% if quote.status > 0 %}
                <a href="{{ path('quote_view', {'id': quote.common.id }) }}" alt="{{ 'Print'|trans }}" class="btn btn-mini"><i class="icon-file"></i> {{ 'View'|trans }}</a>
                {% endif %}
                {% if quote.common.deliveryNote %}
                    <a href="{{ path('delivery_note_view', {'id': quote.common.id }) }}" alt="{{ 'View delivery note'|trans }}" class="btn btn-mini btn-info"><i class="icon-file"></i> {{ 'Order view'|trans }}</a>
                {% endif %}
                {% if quote.common.invoice %}
                    <a href="{{ path('invoice_view', {'id': quote.common.id }) }}" alt="{{ 'View invoice'|trans }}" class="btn btn-mini btn-info"><i class="icon-file"></i> {{ 'Invoice view'|trans }}</a>
                {% endif %}
                {% if (quote.status > 0) %}
                    <a href="{{ path('quote_print', {'id': quote.common.id }) }}" alt="{{ 'Print'|trans }}" class="btn btn-mini"><i class="icon-print"></i> {{ 'Print'|trans }}</a>
                    <a href="{{ path('quote_open', {'id': quote.common.id }) }}" alt="{{ '(Re)Open'|trans }}" class="btn btn-small btn-warning"><i class="icon-folder-open"></i> {{ '(Re)Open'|trans }}</a>
                {% endif %}
            </td>
        </tr>
    {% endfor %}
    </tbody>
    <tfoot>
    <tr>
        <td colspan="8">
            {{ knp_pagination_render(quotes, 'TeclliureInvoiceBundle:quote.common:simple_pagination.html.twig') }}
        </td>
    </tr>
    </tfoot>
</table>